name: DriveDB entry - missing or incorrect
description: Report a drive that is missing from drivedb, or has wrong SMART attribute definitions.
labels: [drivedb]
body:
  - type: markdown
    attributes:
      value: |
        ## What is drivedb?

        SMART attribute slots (1–255) have no fixed meaning - each manufacturer assigns them differently. `drivedb.h` matches a drive by model string and optionally by firmware version, then overrides the generic defaults with model-specific `-v` flags (attribute names, raw-value formats, failure thresholds) and `-F` flags (firmware bug workarounds).

        Firmware matching matters when a vendor changed attribute behaviour or introduced a bug in a specific firmware release - the fix can be scoped to only the affected versions instead of the whole model family.

        For USB-ATA bridges, drivedb also sets the `-d` flag (e.g. `-d sat`) so `smartctl` knows which protocol to use to reach the drive behind the bridge.

        Without a matching entry, `smartctl` falls back to generic labels like `Unknown_Attribute`, misreads a raw value - e.g. reporting a temperature as a 48-bit counter - or fails to communicate with a USB drive at all.

        ---

        **Before filing:** test with the latest drivedb first.

        ```bash
        smartctl --drivedb=/path/to/latest/drivedb.h -a /dev/sdX
        ```

        Download the latest: https://github.com/smartmontools/smartmontools/raw/master/drivedb/drivedb.h

        If the problem still appears with the latest drivedb, continue below.

  - type: input
    id: model
    attributes:
      label: Drive model string
      description: >
        Exact string from `smartctl -i` - "Device Model" for ATA, "Product" for USB.
        Copy-paste; do not retype.
      placeholder: "WDC WD40EFRX-68WT0N0"
    validations:
      required: true

  - type: input
    id: family
    attributes:
      label: Drive family (informal name)
      description: Your best guess is fine - e.g. "Western Digital Red Plus", "Samsung 870 EVO".
      placeholder: "Western Digital Red Plus"

  - type: input
    id: firmware
    attributes:
      label: Firmware version
      description: >
        "Firmware Version" line from `smartctl -i`.
      placeholder: "82.00A82"
    validations:
      required: true

  - type: dropdown
    id: interface
    attributes:
      label: Interface
      options:
        - ATA (SATA / PATA)
        - USB-ATA bridge
    validations:
      required: true

  - type: input
    id: usb_bridge
    attributes:
      label: USB bridge chip (USB only)
      description: >
        Reported in `smartctl -i` as "USB ID" or bridge name.
        Leave blank for ATA drives.
      placeholder: "JMicron JMS578  (0x152d:0x0578)"

  - type: checkboxes
    id: problem_type
    attributes:
      label: What is wrong?
      options:
        - label: Wrong or missing attribute name
        - label: Need `-d` flag for protocol to be read.
        - label: Wrong attribute raw-value type (e.g. raw48 vs raw16)
        - label: Wrong or missing failure threshold
        - label: Wrong or missing firmware bug fix flag (-F)
        - label: Drive is completely missing from drivedb
        - label: Other (describe below)
    validations:
      required: true

  - type: textarea
    id: smartctl_output
    attributes:
      label: Full `smartctl -a` output
      description: Wrap in a code block. Redact serial number if you prefer.
      placeholder: |
        ```
        smartctl 7.5 2023-01-01 r5400 ...
        ...
        ```
    validations:
      required: true

  - type: input
    id: datasheet
    attributes:
      label: Datasheet / product page URL (optional)
      description: >
        Link to the manufacturer datasheet or product page. Helps maintainers
        verify attribute definitions without owning the drive.
      placeholder: "https://www.example.com/datasheet/WD-Red-DS.pdf"

  - type: textarea
    id: other_models
    attributes:
      label: Other model numbers in this family (optional)
      description: >
        List any other model strings that belong to the same drive family. One model per line, copied from datasheet or `smartctl -i /dev/sda`.
      placeholder: |
        MEMPEK1W016GA
        MEMPEK1W032GA
        MEMPEI1W016GA
        MEMPEK1J016GA

  - type: input
    id: proposed_regexp
    attributes:
      label: Proposed modelregexp (optional)
      description: >
        POSIX extended regex to match your drive model(s). Use `.` as a
        single-character wildcard where model variants differ - e.g.
        `MEMPE.1.0..GA` matches `MEMPEK1W016GA`, `MEMPEI1J032GA`, etc.
        Test your regex at https://www.debuggex.com/ (select POSIX ERE).
        Leave blank if unsure.
      placeholder: "MEMPE.1.0..GA"

  - type: textarea
    id: preset_v
    attributes:
      label: Suggested `-v` flags (optional)
      description: >
        Overrides ATA attribute names and raw-value formats.
        Format: `-v ID,FORMAT[:BYTEORDER][,NAME]`
      placeholder: |
        -v 190,tempminmax,Airflow_Temperature_Cel
        -v 194,tempminmax,Temperature_Celsius

  - type: input
    id: preset_d
    attributes:
      label: Suggested `-d` flag (optional, USB bridges)
      description: >
        Device/protocol type. Required for USB-ATA bridges so smartctl knows
        how to talk to the drive. Leave blank for direct-attached ATA drives.
        Known values: `sat`, `sat,12`, `usbjmicron`, `usbjmicron,x`, `usbjmicron,p`,
        `usbjmicron,0`, `usbcypress`, `usbprolific`, `usbsunplus`, `sntjmicron`,
        `sntjmicron/sat`, `sntrealtek`, `sntasmedia`.
      placeholder: "sat"

  - type: textarea
    id: preset_F
    attributes:
      label: Suggested `-F` flags (optional)
      description: >
        Firmware bug workarounds observed on this drive. One flag per line.
        Currently known values:
        `xerrorlba` (fix LBA byte order in error log),
        `nologdir` (skip GP/SMART log directory),
        `samsung3` (fix Samsung SMART status response bug).
      placeholder: |
        -F xerrorlba

  - type: input
    id: version
    attributes:
      label: smartmontools version
      description: From `smartctl --version`.
      placeholder: "7.5"
    validations:
      required: true

  - type: markdown
    attributes:
      value: |
        ---
        Want this fixed faster? Send a pull request.

        Adding a drive entry yourself takes about 10 minutes and gets your fix
        merged without waiting for someone else to own a similar drive.

        Step 1 - Fork
        Click the **Fork** button in the top-right corner of the repository:
        https://github.com/smartmontools/smartmontools

        Step 2 - Clone your fork
        ```bash
        git clone https://github.com/<your-username>/smartmontools.git
        cd smartmontools
        ```

        Step 3 - Create a branch
        ```bash
        git checkout -b drivedb-<Model>
        ```

        Step 4 - Add or edit the YAML file
        ```
        ATA:  drivedb/yaml/ata/<vendor>/<family>.yaml
        USB:  drivedb/yaml/usb/<vendor>/<product>.yaml
        ```
        Edit the file so your drive shows the correct attribute names when you run `smartctl -a`.
        See `CONTRIBUTING.md` for field descriptions and an example entry.

        Step 5 - Regenerate and test
        ```bash
        python3 drivedb/yaml_to_drivedb.py > drivedb/drivedb.h
        python3 drivedb/verify_drivedb.py
        smartctl --drivedb=drivedb/drivedb.h -a /dev/sdX
        ```

        Step 6 - Commit and push
        ```bash
        git add drivedb/yaml/<vendor>/<family>.yaml drivedb/drivedb.h
        git commit -m "drivedb: add <Family Name>"
        git push -u origin drivedb-<Model>
        ```

        Step 7 - Open a pull request
        Go to your fork on GitHub. A banner will appear - click **Compare & pull request**.
        The PR template will guide you through the final checklist.
        
